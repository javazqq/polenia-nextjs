export default function handler(req, res) {
  // Handle the request here
  res.status(200).json({ message: 'Hello, World!' });
}
// This is a simple API route that responds with a JSON object
// containing a greeting message. It can be used to test the API functionality